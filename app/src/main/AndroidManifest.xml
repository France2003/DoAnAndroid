<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoAnAndroid"
        tools:targetApi="31">
        <activity
            android:name=".InformationTacGia"
            android:exported="false" />
        <activity
            android:name=".UpdateTacGia"
            android:exported="false" />
        <activity
            android:name=".AddTacgia"
            android:exported="false" />
        <activity
            android:name=".UpdateTheLoai"
            android:exported="false" />
        <activity
            android:name=".InsertTheLoai"
            android:exported="false" />
        <activity
            android:name=".Information"
            android:exported="false" />
        <activity
            android:name=".InsertBook"
            android:exported="false" />
        <activity
            android:name=".UpdateBook"
            android:exported="false" />
        <activity
            android:name=".ThongKe"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".MainActivityMenu" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_main_menu" -->
        <!-- android:theme="@style/Theme.DoAnAndroid" /> -->
        <!-- <activity -->
        <!-- android:name=".menu" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_menu" -->
        <!-- android:theme="@style/Theme.DoAnAndroid" /> -->
        <!-- <activity -->
        <!-- android:name=".Menu" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_menu" -->
        <!-- android:theme="@style/Theme.DoAnAndroid" /> -->
        <activity
            android:name=".VPAdapter"
            android:exported="false" />
        <activity
            android:name=".TheLoai"
            android:exported="false" />
        <activity
            android:name=".TacGiaa"
            android:exported="false" />
        <activity
            android:name=".Login"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>